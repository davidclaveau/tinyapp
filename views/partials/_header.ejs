<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <% if (user !== undefined && user["email"]) { %>
          <a class="nav-item nav-link" style="color:black" href="#">Welcome, <%= user.email %></a>
          <form method="POST" action="/logout">
            <button type="submit" class="btn btn-light" >Logout</button>
          </form>
        <% } else { %>
          <form method="POST" action="/login" style="display: flex; justify-content: end">
            <input type="text" name="username" placeholder="Username" style="padding: .3rem">
            <button type="submit" class="btn btn-light">Login</button>
          </form>
        <% } %>
      </div>
    </div>
  </nav>
</header>